import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    alignItems: 'center'
  },

  playIcon: {
    color: '#375CA4',
  },

  rewindIcon: {
    color: '#375CA4'
  }

}))
