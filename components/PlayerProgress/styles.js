import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    alignItems: 'center'
  },

  timeContainer: {
    display: 'flex'
  },

  time: {
    fontSize: '14px',
    color: '#375CA4'
  }

}))
